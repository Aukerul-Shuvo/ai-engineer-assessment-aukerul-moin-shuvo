# Prometheus for the local observability profile: scrape the API's /metrics every 15 seconds.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: ai-engineer-assessment
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
